{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only deep-link permission so the OS share sheet can hand URLs to Verko.",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": [
    "deep-link:default"
  ]
}
